# ====================================
# CONFIGURACIÓN DEL ENTORNO
# ====================================
# Copia este archivo a .env y configura con tus datos reales
# IMPORTANTE: .env está en .gitignore - nunca lo subas a GitHub

# Entorno: local | producción
APP_ENV=local

# URL base de la aplicación (se auto-detecta si no se especifica)
# APP_BASE_URL=http://localhost

# ====================================
# BASE DE DATOS
# ====================================
DB_HOST=localhost
DB_NAME=inmobiliaria_db
DB_USER=root
DB_PASS=

# ====================================
# CONFIGURACIÓN SMTP
# ====================================
# Servidor SMTP y puerto
SMTP_HOST=smtp.example.com
SMTP_PORT=587

# Seguridad: tls | ssl | none
# - tls: Puerto 587 (recomendado)
# - ssl: Puerto 465
# - none: Puerto 25 (NO recomendado, sin cifrado)
SMTP_SECURE=tls

# Credenciales de autenticación SMTP
SMTP_USER=tu-email@example.com
SMTP_PASS=tu-password-smtp

# ====================================
# EMAILS DE DESTINO
# ====================================
# Email de la agencia (recibe leads de tasación)
LEAD_AGENCY_EMAIL=agencia@example.com

# Email para envíos automáticos (opcional, usa SMTP_USER si no se especifica)
NOREPLY_EMAIL=noreply@example.com

# ====================================
# EJEMPLOS DE CONFIGURACIÓN SMTP POPULARES
# ====================================

# --- Gmail ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=tls
# SMTP_USER=tucuenta@gmail.com
# SMTP_PASS=tu-app-password
# NOTA: Gmail requiere "App Password" (https://support.google.com/accounts/answer/185833)
# NO uses tu contraseña normal de Gmail

# --- Outlook / Office 365 ---
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=tls
# SMTP_USER=tu-cuenta@outlook.com
# SMTP_PASS=tu-password

# --- cPanel / Hosting Compartido ---
# SMTP_HOST=mail.tudominio.com
# SMTP_PORT=587
# SMTP_SECURE=tls
# SMTP_USER=noreply@tudominio.com
# SMTP_PASS=password-del-email

# --- SendGrid (API SMTP) ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=tls
# SMTP_USER=apikey
# SMTP_PASS=TU_API_KEY_DE_SENDGRID

# ====================================
# NOTAS IMPORTANTES
# ====================================
# 1. Si los correos caen en spam, configura SPF/DKIM/DMARC en tu dominio
# 2. Gmail tiene límite de 500 emails/día para cuentas gratuitas
# 3. En producción, usa SMTP de tu hosting o un servicio dedicado
# 4. NUNCA expongas este archivo .env públicamente
# 5. En producción, usa permisos restrictivos: chmod 600 .env
